% ==> zslide bug and '\pin' command
\documentclass[
  layout={slide, aspect=16|9},
]{ztex}
\usepackage[library={python, cache}]{ztikz}
\ztoolloadlib{zdraw}
\usepackage{lipsum}
\zslidelogo{example-image-a}
\newcommand{\pin}[1]{%
  \zpagemask[layer=foreground, position={(0pt, 0pt)}, anchor=lb]{%
    \begin{zpic}[unit=\zpw]#1\end{zpic}%
  }%
}
\newcommand{\zscale}[2][1]{\ztoolboxaffine{#2}{#1, 0, 0, #1}}
% \ShowHook{shipout/background}
% >     zslide-background, ztex, ztex@page@mask-DEFAULT.



\title{Test \ztex}
\author{Eureka}
\date{\today}
\begin{document}
\maketitle
\section{FIRST SECTION}
\lipsum[1][1-6]


\begin{pyfig}{sin_graph.pdf}
import matplotlib
matplotlib.use('Agg')
from matplotlib import pyplot as plt
import numpy as np
x = np.linspace(0, 2*np.pi, num = 80)
y = np.sin(x)*np.cos(x)+.2
y2 = np.sin(2*x)*np.cos(x/2)+.5
plt.plot(x, y, 'r-*')
plt.plot(x, y2, 'b-o')
\end{pyfig}
\edef\sinGraph{\pyfigOutputFile}
\begin{pyfig}{heatmap.pdf}
import matplotlib.pyplot as plt
import numpy as np
import matplotlib
matplotlib.use('Agg')
import matplotlib as mpl

vegetables = ["cucumber", "tomato", "lettuce", "asparagus",
              "potato", "wheat", "barley"]
farmers = ["Farmer Joe", "Upland Bros.", "Smith Gardening",
           "Agrifun", "Organiculture", "BioGoods Ltd.", "Cornylee Corp."]

harvest = np.array([[0.8, 2.4, 2.5, 3.9, 0.0, 4.0, 0.0],
                    [2.4, 0.0, 4.0, 1.0, 2.7, 0.0, 0.0],
                    [1.1, 2.4, 0.8, 4.3, 1.9, 4.4, 0.0],
                    [0.6, 0.0, 0.3, 0.0, 3.1, 0.0, 0.0],
                    [0.7, 1.7, 0.6, 2.6, 2.2, 6.2, 0.0],
                    [1.3, 1.2, 0.0, 0.0, 0.0, 3.2, 5.1],
                    [0.1, 2.0, 0.0, 1.4, 0.0, 1.9, 6.3]])

fig, ax = plt.subplots()
im = ax.imshow(harvest)

# Show all ticks and label them with the respective list entries
ax.set_xticks(range(len(farmers)), labels=farmers,
              rotation=45, ha="right", rotation_mode="anchor")
ax.set_yticks(range(len(vegetables)), labels=vegetables)

# Loop over data dimensions and create text annotations.
for i in range(len(vegetables)):
    for j in range(len(farmers)):
        text = ax.text(j, i, harvest[i, j],
                      ha="center", va="center", color="w")

ax.set_title("Harvest of local farmers (in tons/year)")
fig.tight_layout()
\end{pyfig}
\edef\heatmap{\pyfigOutputFile}


\pin{
  \zvector[draw=blue](.32, .2)(.5, .05)
    \put(.5, .05){\zscale[.5]{\boxed{f(x) = \sin(2x)*\cos(x/2)}}}
  \zvector[draw=red](.39, .165)(.5, .08)
    \put(.5, .08){\zscale[.5]{\boxed{g(x) = \sin(x)*\cos(x)}}}
}
\begin{center}
\ztoolboxaffine{\includegraphics[width=.35\linewidth]{\sinGraph}}{1, 0, .75, 1}
\includegraphics[width=.35\linewidth]{\heatmap}
\end{center}


\lipsum[2][1-4]
\begin{pyfig}{sigma.pdf}
import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt
import numpy as np

t = np.linspace(-10, 10, 100)
sig = 1 / (1 + np.exp(-t))

fig, ax = plt.subplots()
ax.axhline(y=0, color="black", linestyle="--")
ax.axhline(y=0.5, color="black", linestyle=":")
ax.axhline(y=1.0, color="black", linestyle="--")
ax.axvline(color="grey")
ax.axline((0, 0.5), slope=0.25, color="black", linestyle=(0, (5, 5)))
ax.plot(t, sig, linewidth=2, label=r"$\sigma(t) = \frac{1}{1 + e^{-t}}$")
ax.set(xlim=(-10, 10), xlabel="t")
ax.legend(fontsize=14)
\end{pyfig}

And is shown below
\begin{center}
\includegraphics[width=.4\linewidth]{\pyfigOutputFile}
\end{center}

The graph of the $\sigma$-function is 
\[
\sigma(t) = \frac{1}{1 + e^{-t}}
\]
\end{document}



% ==> change zslide title box to rounded rectangle
\InputIfFileExists{zlatex-cfg.tex}{}{}
\documentclass[
  layout={slide, aspect=16|9},
]{../code/ztex}

\title{Rounded corner style Title Page}
\author{Eureka\quad and \quad \ztex{} Eureka}
\date{\today}
\begin{document}
\maketitle

\section{FIRST}
The FIRST section.
\end{document}